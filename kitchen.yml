# -*- coding: utf-8 -*-
# vim: ft=yaml
---
# For help on this file's format, see https://kitchen.ci/
driver:
  name: docker
  use_sudo: false
  privileged: true
  run_command: /usr/lib/systemd/systemd

provisioner:
  name: salt_solo
  log_level: debug
  salt_install: none
  require_chef: false
  formula: mongodb
  salt_copy_filter:
    - .kitchen
    - .git

platforms:
  ## SALT `master`
  - name: ubuntu-2204-master-py3
    driver:
      image: saltimages/salt-master-py3:ubuntu-22.04
      run_command: /lib/systemd/systemd
  - name: opensuse-tmbl-latest-master-py3
    driver:
      image: saltimages/salt-master-py3:opensuse-tumbleweed-latest
    # Workaround to avoid intermittent failures on `opensuse-tumbleweed`:
    # => SCP did not finish successfully (255):  (Net::SCP::Error)
    transport:
      max_ssh_sessions: 1
  - name: oraclelinux-8-master-py3
    driver:
      image: saltimages/salt-master-py3:oraclelinux-8
  - name: arch-base-latest-master-py3
    driver:
      image: saltimages/salt-master-py3:arch-base-latest
  - name: gentoo-stage3-latest-master-py3
    driver:
      image: saltimages/salt-master-py3:gentoo-stage3-latest
      run_command: /sbin/init
  - name: gentoo-stage3-systemd-master-py3
    driver:
      image: saltimages/salt-master-py3:gentoo-stage3-systemd
  - name: almalinux-8-master-py3
    driver:
      image: saltimages/salt-master-py3:almalinux-8
  - name: rockylinux-8-master-py3
    driver:
      image: saltimages/salt-master-py3:rockylinux-8
      # Kitchen transport workaround (not a MongoDB/Salt runtime
      # requirement - do not propagate to production-shaped tests):
      # the GHA Ubuntu host's apparmor profile for `unix-chkpwd`
      # strips its dac_override capability inside containers, so
      # sshd's PAM account check can't read /etc/shadow and rejects
      # every pre-auth ("could not obtain user info (kitchen)").
      # Kitchen only needs SSH pubkey transport into an ephemeral
      # container, so disable PAM in sshd entirely - smaller blast
      # radius than `security_opt: apparmor:unconfined`.
      provision_command:
        - dnf install -y openssh-server sudo
        - ssh-keygen -A
        - getent passwd kitchen >/dev/null
          || useradd -m -s /bin/bash kitchen
        - usermod -p '*' kitchen
        - sed -ri 's/^[#[:space:]]*UsePAM[[:space:]]+.*/UsePAM no/'
          /etc/ssh/sshd_config
        - grep -qE '^[[:space:]]*UsePAM[[:space:]]+no'
          /etc/ssh/sshd_config
          || echo 'UsePAM no' >> /etc/ssh/sshd_config
        - systemctl enable sshd
        - install -d -m 0700 -o kitchen -g kitchen /home/kitchen/.ssh
        - echo 'kitchen ALL=(ALL) NOPASSWD:ALL' > /etc/sudoers.d/kitchen
        - chmod 0440 /etc/sudoers.d/kitchen

verifier:
  # https://www.inspec.io/
  name: inspec
  sudo: true
  reporter:
    # cli, documentation, html, progress, json, json-min, json-rspec, junit
    - cli

suites:
  - name: default
    provisioner:
      state_top:
        base:
          '*':
            - mongodb._mapdata
            - mongodb
      pillars:
        top.sls:
          base:
            '*':
              - mongodb
      pillars_from_files:
        mongodb.sls: test/salt/pillar/default.sls
    verifier:
      inspec_tests:
        - path: test/integration/default
